{
  "name": "aarogya-foundation",
  "version": "1.0.0",
  "private": true,
  "description": "Aarogya Foundation — full-stack MERN web app for a health/humanitarian NGO (donations, volunteers, mini-CMS, admin dashboard).",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:mem": "concurrently -n server,client -c green,cyan \"npm:dev:mem:server\" \"npm:dev:client\"",
    "dev:mem:server": "npm run dev:mem --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server",
    "test": "npm run test --workspace server && npm run test --workspace client",
    "lint": "npm run lint --workspace server && npm run lint --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
